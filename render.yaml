services:
  - type: web
    name: django-app
    runtime: python
    buildCommand: |
      pip install -r requirements.txt
      python manage.py collectstatic --noinput
      python manage.py migrate --noinput
    startCommand: gunicorn config.wsgi:application --bind 0.0.0.0:$PORT
    envVars:
      - key: SECRET_KEY
        generateValue: true
      - key: DEBUG
        value: "False"
      - key: ALLOWED_HOSTS
        value: dpg-d0iu8cq4d50c73dvtj50-a.oregon-postgres.render.com  # 🛠 Поставь реальное имя, не your-app-name!
      - key: DATABASE_URL
        fromDatabase:
          name: dbname_a2fl  # 🛠 Поменяй на точное имя БД в Render
          property: connectionString